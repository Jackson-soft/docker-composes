version: "3.7"

services:
    dashboard:
        build:
            context: ./dashboard
            dockerfile: Dockerfile
        restart: always
        ports:
            - 9000:9000
        volumes:
            - ./dashboard_conf/conf.yaml:/usr/local/apisix-dashboard/conf/conf.yaml
